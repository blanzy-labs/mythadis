# Demo Script

## Foundation Demo

1. Start the backend and show `/health` returning `status: ok`.
2. Start the frontend at `http://localhost:5173`.
3. Show the title, tagline, and placeholder workflow message.
4. Click the backend health check button.
5. Stop the backend and show the friendly unavailable message.
6. Run `docker compose up --build` and verify both services start.
